import { HeaderWrapper } from '../../components/HeaderWrapper';

export default function Page() {
  return (
    <HeaderWrapper title='Settings' subTitle=''>
      Settings
    </HeaderWrapper>
  );
}
